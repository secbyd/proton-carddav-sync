diff --git a/auth.go b/auth.go
index 9cfc639..ff89f85 100644
--- a/auth.go
+++ b/auth.go
@@ -6,15 +6,19 @@ import (
 	"github.com/go-resty/resty/v2"
 )
 
+// PATCH(secbyd/proton-carddav-sync): all auth endpoints use the v3 API
+// (/auth/...) instead of /auth/v4/...; the v4 auth flow is CAPTCHA-gated while
+// the legacy v3 flow used by hydroxide/ferroxide is not. See
+// patches/go-proton-api-auth-v3.patch.
 func (c *Client) Auth2FA(ctx context.Context, req Auth2FAReq) error {
 	return c.do(ctx, func(r *resty.Request) (*resty.Response, error) {
-		return r.SetBody(req).Post("/auth/v4/2fa")
+		return r.SetBody(req).Post("/auth/2fa")
 	})
 }
 
 func (c *Client) AuthDelete(ctx context.Context) error {
 	return c.do(ctx, func(r *resty.Request) (*resty.Response, error) {
-		return r.Delete("/auth/v4")
+		return r.Delete("/auth")
 	})
 }
 
@@ -24,7 +28,7 @@ func (c *Client) AuthSessions(ctx context.Context) ([]AuthSession, error) {
 	}
 
 	if err := c.do(ctx, func(r *resty.Request) (*resty.Response, error) {
-		return r.SetResult(&res).Get("/auth/v4/sessions")
+		return r.SetResult(&res).Get("/auth/sessions")
 	}); err != nil {
 		return nil, err
 	}
@@ -34,12 +38,12 @@ func (c *Client) AuthSessions(ctx context.Context) ([]AuthSession, error) {
 
 func (c *Client) AuthRevoke(ctx context.Context, authUID string) error {
 	return c.do(ctx, func(r *resty.Request) (*resty.Response, error) {
-		return r.Delete("/auth/v4/sessions/" + authUID)
+		return r.Delete("/auth/sessions/" + authUID)
 	})
 }
 
 func (c *Client) AuthRevokeAll(ctx context.Context) error {
 	return c.do(ctx, func(r *resty.Request) (*resty.Response, error) {
-		return r.Delete("/auth/v4/sessions")
+		return r.Delete("/auth/sessions")
 	})
 }
diff --git a/manager_auth.go b/manager_auth.go
index 35dde25..c778493 100644
--- a/manager_auth.go
+++ b/manager_auth.go
@@ -73,7 +73,9 @@ func (m *Manager) AuthInfo(ctx context.Context, req AuthInfoReq) (AuthInfo, erro
 		AuthInfo
 	}
 
-	if _, err := m.r(ctx).SetBody(req).SetResult(&res).Post("/auth/v4/info"); err != nil {
+	// PATCH(secbyd/proton-carddav-sync): use the v3 auth endpoint, which is not
+	// CAPTCHA-gated like /auth/v4 (see patches/go-proton-api-auth-v3.patch).
+	if _, err := m.r(ctx).SetBody(req).SetResult(&res).Post("/auth/info"); err != nil {
 		return AuthInfo{}, err
 	}
 
@@ -83,7 +85,7 @@ func (m *Manager) AuthInfo(ctx context.Context, req AuthInfoReq) (AuthInfo, erro
 func (m *Manager) AuthModulus(ctx context.Context) (AuthModulus, error) {
 	var res AuthModulus
 
-	if _, err := m.r(ctx).SetResult(&res).Get("/auth/v4/modulus"); err != nil {
+	if _, err := m.r(ctx).SetResult(&res).Get("/auth/modulus"); err != nil {
 		return AuthModulus{}, err
 	}
 
@@ -95,7 +97,7 @@ func (m *Manager) auth(ctx context.Context, req AuthReq) (Auth, error) {
 		Auth
 	}
 
-	if _, err := m.r(ctx).SetBody(req).SetResult(&res).Post("/auth/v4"); err != nil {
+	if _, err := m.r(ctx).SetBody(req).SetResult(&res).Post("/auth"); err != nil {
 		return Auth{}, err
 	}
 
@@ -121,7 +123,7 @@ func (m *Manager) authRefresh(ctx context.Context, uid, ref string) (Auth, error
 		Auth
 	}
 
-	if resp, err := m.r(ctx).SetBody(req).SetResult(&res).Post("/auth/v4/refresh"); err != nil {
+	if resp, err := m.r(ctx).SetBody(req).SetResult(&res).Post("/auth/refresh"); err != nil {
 		if resp != nil {
 			return Auth{}, &resty.ResponseError{Response: resp, Err: err}
 		}
